import React from 'react';

const Wrapper: React.FC = () => {
    const style = {
        width: '100%',
        height: '100%',
        display: 'flex'
    };

    return <div style={style} />;
};

export default Wrapper;